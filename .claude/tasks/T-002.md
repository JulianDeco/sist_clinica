# T-002 — Scaffold frontend Angular 18 + Angular Material

## Estado: 🔄 en_progreso — cierre pendiente (estado vivo en tasks.json)
## Fase: 1 — Infra, Auth y Multitenant
## Agente: frontend-developer
## Prioridad: alta
## Fecha inicio: 2026-06-09

## Registro de lo entregado

Spec `docs/specifications/T-002-scaffold-frontend.spec.md` APPROVED (2026-06-09)
antes de codificar. Workspace Angular 18 standalone (sin NgModules) en
`src/frontend/`, gestionado con pnpm.

- **Estructura**: `core/` (api, auth, interceptors, tenant), `shared/`,
  `features/` (lazy), `layout/` — según `docs/standards/02-frontend-standards.md`.
- **Angular Material** con tema azul médico (`mat.$azure-palette` +
  `mat.$cyan-palette` en `styles.scss`).
- **AuthService** con máquina de estados 3 fases (ADR-014):
  UNAUTHENTICATED → IDENTITY_CONFIRMED → READY. Estado con Signals,
  tokens solo en memoria. Auto-select si el usuario tiene un solo tenant;
  `switchTenant()` sin re-login.
- **Interceptores**: auth (Bearer), tenant, error (401 → limpiar estado).
- **Guards**: `authGuard`, `roleGuard`, `selectTenantGuard`.
- **Rutas lazy**: `/` landing → `/login` → `/select-tenant` → `/app/*`
  (MainLayout) con hijos agenda/patients/clinical/reports/settings
  (placeholders) + unauthorized.
- **Componentes**: LandingComponent, LoginComponent, SelectTenantComponent,
  UnauthorizedComponent, MainLayout + Navbar (tenant switcher) + Sidebar.
- **Migración V010** (refactor `users` multi-tenant: tabla `user_tenants`
  según ADR-014) incluida en el alcance.
- Config de workspace: `.editorconfig`, `.gitignore`, `.npmrc`, README.

## Pendiente para cierre

- `ng test` en verde + integración de la branch (estado → `testeando` → `hecho`).
- La conexión real al backend de auth depende de T-003 (endpoints JWT).

## Referencias

- `docs/specifications/T-002-scaffold-frontend.spec.md`
- `docs/adr/ADR-014-multi-tenant-membership.md`
- `.claude/context/modules/07-frontend.md` — contexto del módulo
