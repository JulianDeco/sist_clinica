import { Component } from '@angular/core';

/** Placeholder — implementación en T-0XX. */
@Component({
  selector: 'app-settings-placeholder',
  standalone: true,
  template: '<p style="padding:24px">Módulo Settings — próximamente.</p>',
})
export class SettingsPlaceholderComponent {}
